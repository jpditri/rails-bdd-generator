<!-- Generated by Rails BDD Generator with UX Enhancements -->
<div class="container">
  <div class="row">
    <div class="col-12">
      <div class="card">
        <div class="card-header">
          <h1 style="margin: 0;">📚 Books</h1>
        </div>
        <div class="card-body">
          <div style="margin-bottom: 1rem; display: flex; justify-content: space-between; align-items: center;">
            <%= render 'shared/search' %>
            <%= link_to '+ New Book', new_book_path, class: 'btn btn-primary' %>
          </div>

          <div class="table-responsive">
            <table class="table">
              <thead>
                <tr>
                  <th>Title</th>
                  <th>Author</th>
                  <th>Price</th>
                  <th>Stock</th>
                  <th style="text-align: right;">Actions</th>
                </tr>
              </thead>
              <tbody>
                <% @books.each do |book| %>
                  <tr class="book-row">
                    <td><%= truncate(book.title.to_s, length: 50) %></td>
                    <td><%= truncate(book.author.to_s, length: 50) %></td>
                    <td class="price"><%= format_currency(book.price) %></td>
                    <td>
                      <% if book.stock_quantity > 10 %>
                        <span class="badge badge-success">In Stock (<%= book.stock_quantity %>)</span>
                      <% elsif book.stock_quantity > 0 %>
                        <span class="badge badge-warning">Low Stock (<%= book.stock_quantity %>)</span>
                      <% else %>
                        <span class="badge badge-danger">Out of Stock</span>
                      <% end %>
                    </td>
                    <td style="text-align: right;">
                      <%= link_to 'View', book, class: 'btn btn-sm btn-outline' %>
                      <%= link_to 'Edit', edit_book_path(book), class: 'btn btn-sm btn-secondary' %>
                      <%= link_to 'Delete', book, method: :delete,
                          data: { confirm: 'Are you sure?' },
                          class: 'btn btn-sm btn-danger' %>
                    </td>
                  </tr>
                <% end %>
                <% if @books.empty? %>
                  <tr>
                    <td colspan="5" style="text-align: center; padding: 2rem; color: var(--muted);">
                      No books found. <%= link_to 'Add your first book', new_book_path %>.
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>

          <%= paginate @books %>
        </div>
      </div>
    </div>
  </div>
</div>