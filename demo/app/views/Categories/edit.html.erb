<div class="container">
  <div class="card">
    <div class="card-header">
      <h1 style="margin: 0;">Edit Category</h1>
    </div>
    <div class="card-body">
      <%= render 'form', Category: @Category %>
    </div>
    <div class="card-footer">
      <%= link_to @Category, class: 'btn btn-outline' do %>
        <%= view_icon %> View
      <% end %>
      <%= link_to Categories_path, class: 'btn btn-secondary' do %>
        <%= list_icon %> Back to List
      <% end %>
    </div>
  </div>
</div>
